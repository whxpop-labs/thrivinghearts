# This config was automatically generated from your source code
# Stacks detected: deps:node:.,package_manager:yarn:
version: 2.1
orbs:
  node: circleci/node@5
  typescript: vidsy/typescript@1.2.1
  slack: circleci/slack@4.1
jobs:
  cda-compiler-build:
    # Build node project
    executor: node/default
    steps:
      - slack/notify:
          channel: doodley
          custom: |
            {
              "blocks": [
                {
                  "type": "selection",
                  "fields": [
                    {
                      "type": "plain_text",
                      "text": "*Doodley CDA Compiler*: Build Started.",
                      "emoji": true
                    }
                  ]
                }
              ]
            }
          event: always
          thread_id: deployment
      - checkout
      - node/install-packages:
          pkg-manager: yarn
      - run:
          command: npx tsc
          name: Build TypeScript
      - run:
          name: Create the ~/artifacts directory if it doesn't exist
          command: mkdir -p ~/artifacts
      - slack/notify:
          channel: doodley
          custom: |
            {
              "blocks": [
                {
                  "type": "selection",
                  "fields": [
                    {
                      "type": "plain_text",
                      "text": "*Doodley CDA Compiler*: Build Finished.",
                      "emoji": true
                    }
                  ]
                }
              ]
            }
          event: always
          thread_id: deployment
      # Copy output to artifacts dir
      - slack/notify:
          channel: doodley
          custom: |
            {
              "blocks": [
                {
                  "type": "selection",
                  "fields": [
                    {
                      "type": "plain_text",
                      "text": "*Doodley CDA Compiler*: Artifact Upload Started.",
                      "emoji": true
                    }
                  ]
                }
              ]
            }
          event: always
          thread_id: deployment
      - run:
          name: Copy artifacts
          command: cp scripts/compile.js ~/artifacts/compile-cda-app.js
      - store_artifacts:
          path: ~/artifacts/compile-cda-app.js
          destination: compile.js
      - slack/notify:
          channel: doodley
          custom: |
            {
              "blocks": [
                {
                  "type": "selection",
                  "fields": [
                    {
                      "type": "plain_text",
                      "text": "*Doodley CDA Compiler*: Artifact Upload Finished.",
                      "emoji": true
                    }
                  ]
                }
              ]
            }
          event: always
          thread_id: deployment
workflows:
  build:
    jobs:
      - build-node
    # - deploy:
    #     requires:
    #       - build-node
